automation: stamp_form_submitted
name: A request filed from a form was submitted when the form was
when:
  field: submission
  operator: isNotEmpty
effects:
  - type: updateRecord
    set:
      submitted_at: '{{now}}'
    setIfEmpty: true
trigger: record.created
entity: time_off

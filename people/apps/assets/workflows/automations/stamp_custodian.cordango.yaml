automation: stamp_custodian
name: Whoever registers a thing looks after it until somebody else does
effects:
  - type: updateRecord
    set:
      custodian: '{{actor.id}}'
    setIfEmpty: true
trigger: record.created
entity: asset

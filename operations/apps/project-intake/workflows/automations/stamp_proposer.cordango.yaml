automation: stamp_proposer
name: A proposal belongs to whoever wrote it
effects:
  - type: updateRecord
    set:
      requested_by: '{{actor.id}}'
    setIfEmpty: true
trigger: record.created
entity: project

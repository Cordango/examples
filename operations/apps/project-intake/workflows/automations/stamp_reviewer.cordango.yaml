automation: stamp_reviewer
name: A score belongs to whoever gave it
effects:
  - type: updateRecord
    set:
      reviewer: '{{actor.id}}'
      reviewed_at: '{{now}}'
    setIfEmpty: true
trigger: record.created
entity: intake_review
